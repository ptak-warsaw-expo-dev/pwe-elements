# `backend/load-more-posts.php`

Plik first-party PWE Elements w kategorii `backend`.

## Metadane

- **Kategoria:** `backend`
- **Rozmiar:** 3363 B
- **Liczba linii:** 92
- **Źródło:** `backend/load-more-posts.php`

## Klasy i metody

- Brak klas.

## Funkcje globalne

- `load_more_posts()` — linia 4

## Rejestracje WordPress / GF

- **action:** `wp_ajax_load_more_posts` — linia 91
- **action:** `wp_ajax_nopriv_load_more_posts` — linia 92

## Wybrane wywołania statyczne

- Brak.

## API WordPress rozpoznane heurystycznie

- `get_the_ID()`
- `wp_strip_all_tags()`
- `get_permalink()`
- `has_post_thumbnail()`
- `get_the_post_thumbnail_url()`
- `get_the_title()`
- `get_the_date()`
- `get_locale()`
- `wp_die()`
- `add_action()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
