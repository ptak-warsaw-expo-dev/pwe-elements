# `scripts/speakers-slider.php`

Plik first-party PWE Elements w kategorii `script-helper`.

## Metadane

- **Kategoria:** `script-helper`
- **Rozmiar:** 27616 B
- **Liczba linii:** 469
- **Źródło:** `scripts/speakers-slider.php`

## Klasy i metody

### `PWESpeakersSlider` — linia 3

  - `public __construct()` — linia 8
  - `private static createDOM($id_rnd, $media_url, $min_image, $max_image, $options)` — linia 20
  - `private static generateScript($id_rnd, $media_url, $min_image, $slide_speed, $options)` — linia 200
  - `public static sliderOutput($media_url, $slide_speed = 3000, $info_speakers_options)` — linia 444

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `self::createDOM()`
- `self::generateScript()`

## API WordPress rozpoznane heurystycznie

- `do_shortcode()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
