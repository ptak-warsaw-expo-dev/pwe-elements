# `elements/promote-yourself.php`

Plik first-party PWE Elements w kategorii `legacy-element`.

## Metadane

- **Kategoria:** `legacy-element`
- **Rozmiar:** 18783 B
- **Liczba linii:** 476
- **Źródło:** `elements/promote-yourself.php`

## Klasy i metody

### `PWElementPromot` — linia 7

  - `public __construct()` — linia 14
  - `public static initElements()` — linia 23
  - `public static multi_translation($key)` — linia 54
  - `public static render_template($template, $vars = array())` — linia 96
  - `public static output($atts)` — linia 115

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `self::findColor()`
- `self::adjustBrightness()`
- `self::findBestLogo()`
- `PWECommonFunctions::get_database_groups_data()`
- `PWECommonFunctions::get_database_groups_contacts_data()`
- `self::findAllImages()`
- `self::multi_translation()`
- `self::render_template()`
- `self::languageChecker()`

## API WordPress rozpoznane heurystycznie

- `get_locale()`
- `do_shortcode()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
