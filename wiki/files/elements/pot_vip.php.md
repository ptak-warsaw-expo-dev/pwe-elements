# `elements/pot_vip.php`

Plik first-party PWE Elements w kategorii `legacy-element`.

## Metadane

- **Kategoria:** `legacy-element`
- **Rozmiar:** 19159 B
- **Liczba linii:** 436
- **Źródło:** `elements/pot_vip.php`

## Klasy i metody

### `PWElementConfirmationVip` — linia 7

  - `public __construct()` — linia 13
  - `public static initElements()` — linia 21
  - `public static output($atts)` — linia 46

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `self::findFormsID()`
- `PWECommonFunctions::transform_dates()`
- `self::isTradeDateExist()`
- `self::languageChecker()`
- `GFAPI::get_forms()`

## API WordPress rozpoznane heurystycznie

- `shortcode_atts()`
- `do_shortcode()`
- `get_locale()`

## Dołączane pliki / wyrażenia include

- `s("' . $input_name . '") && !labelText.includes("id")) {`
- `s("' . $input_email . '") && !labelText.includes("id")) {`
- `s("' . $input_phone . '") && !labelText.includes("id")) {`
- `s("' . $input_company . '")) {`
- `s("' . $input_channel . '")) {`
- `s("' . $input_badge . '")) {`
- `s("' . $input_id . '") && !labelText.includes("name") && !labelText.includes("email") && !labelText.includes("phone")) {`
- `s("' . $input_idname . '")) {`
- `s("' . $input_idemail . '")) {`
- `s("' . $input_idphone . '")) {`

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
