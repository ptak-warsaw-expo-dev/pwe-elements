# `elements/conference_cap.php`

Plik first-party PWE Elements w kategorii `legacy-element`.

## Metadane

- **Kategoria:** `legacy-element`
- **Rozmiar:** 32267 B
- **Liczba linii:** 749
- **Źródło:** `elements/conference_cap.php`

## Klasy i metody

### `PWElementConferenceCap` — linia 10

  - `public __construct()` — linia 16
  - `public static initElements()` — linia 24
  - `public static connectToDatabase()` — linia 97
  - `public static getDatabaseDataConferences()` — linia 122
  - `private static speakerImageMini($speaker_images)` — linia 143
  - `public static output($atts)` — linia 238

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `self::connectToDatabase()`
- `self::getDatabaseDataConferences()`
- `PWECommonFunctions::decode_clean_content()`
- `self::speakerImageMini()`

## API WordPress rozpoznane heurystycznie

- `esc_url()`
- `shortcode_atts()`
- `esc_html()`
- `esc_attr()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
