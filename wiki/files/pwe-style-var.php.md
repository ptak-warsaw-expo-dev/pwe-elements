# `pwe-style-var.php`

Plik first-party PWE Elements w kategorii `core`.

## Metadane

- **Kategoria:** `core`
- **Rozmiar:** 5762 B
- **Liczba linii:** 203
- **Źródło:** `pwe-style-var.php`

## Klasy i metody

### `PWEStyleVar` — linia 3

  - `public pwe_enqueue_style_var()` — linia 8
  - `private get_color_map()` — linia 15
  - `public init_color_shortcodes()` — linia 31
  - `private get_color($key, $type = 'base')` — linia 65
  - `public handle_color_mix($atts)` — linia 99
  - `public pwe_style_var()` — linia 170

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `self::pwe_color()`

## API WordPress rozpoznane heurystycznie

- `add_shortcode()`
- `shortcode_exists()`
- `do_shortcode()`
- `shortcode_atts()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
