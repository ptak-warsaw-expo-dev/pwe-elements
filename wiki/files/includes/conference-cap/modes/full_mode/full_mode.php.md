# `includes/conference-cap/modes/full_mode/full_mode.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 7083 B
- **Liczba linii:** 174
- **Źródło:** `includes/conference-cap/modes/full_mode/full_mode.php`

## Klasy i metody

### `PWEConferenceCapFullMode` — linia 6

  - `public __construct()` — linia 8
  - `public static output($atts, $sessions, $conf_function, &$speakersDataMapping, &$all_day_speakers, $short_day, $conf_slug_index, $panel, $conf_location, $prelegent_show)` — linia 10
### `PWE_Conference_Cap_Full_Mode` — linia 149

  - `public render(array $context)` — linia 151
  - `public get_assets()` — linia 169

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `conf_function::speakerImageMini()`
- `PWEConferenceCapFullMode::output()`

## API WordPress rozpoznane heurystycznie

- `esc_attr()`
- `esc_html()`
- `wp_kses_post()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
