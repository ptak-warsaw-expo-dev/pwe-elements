# `includes/conference-cap/modes/medal_ceremony/medal_ceremony.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 1882 B
- **Liczba linii:** 51
- **Źródło:** `includes/conference-cap/modes/medal_ceremony/medal_ceremony.php`

## Klasy i metody

### `PWEConferenceCapMedalCeremony` — linia 6

  - `public __construct()` — linia 8
  - `public static output($atts, $sessions, $conf_function, $conf_name, $day, $conf_slug, $conf_location)` — linia 10
### `PWE_Conference_Cap_Medal_Ceremony` — linia 32

  - `public render(array $context)` — linia 34
  - `public get_assets()` — linia 46

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `PWECommonFunctions::languageChecker()`
- `PWEConferenceCapMedalCeremony::output()`

## API WordPress rozpoznane heurystycznie

- `wp_kses_post()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
