# `includes/conference-cap/core/shortcode.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 718 B
- **Liczba linii:** 24
- **Źródło:** `includes/conference-cap/core/shortcode.php`

## Klasy i metody

### `PWE_Conference_Cap_Shortcode` — linia 6

  - `public render(array $atts)` — linia 13

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `PWE_Conference_Cap_Attributes::from_shortcode()`
- `PWE_Conference_Cap_Mode_Resolver::resolve_mode()`
- `PWE_Conference_Cap_Assets::enqueue_common()`
- `PWE_Conference_Cap_Assets::enqueue_mode()`
- `PWE_Conference_Cap_Legacy_Renderer::PWEConferenceCapOutput()`

## API WordPress rozpoznane heurystycznie

- Brak.

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
