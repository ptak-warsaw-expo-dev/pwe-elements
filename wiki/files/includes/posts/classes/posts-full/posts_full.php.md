# `includes/posts/classes/posts-full/posts_full.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 20442 B
- **Liczba linii:** 441
- **Źródło:** `includes/posts/classes/posts-full/posts_full.php`

## Klasy i metody

### `PWEPostsFull` — linia 7

  - `public __construct()` — linia 13
  - `public output($atts)` — linia 23

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `PWECommonFunctions::lang_pl()`
- `self::languageChecker()`

## API WordPress rozpoznane heurystycznie

- `wp_enqueue_style()`
- `plugin_dir_url()`
- `plugin_dir_path()`
- `shortcode_atts()`
- `get_term_by()`
- `get_the_ID()`
- `wp_strip_all_tags()`
- `get_permalink()`
- `has_post_thumbnail()`
- `get_the_post_thumbnail_url()`
- `get_the_title()`
- `get_the_date()`
- `get_locale()`
- `wp_reset_postdata()`
- `wp_count_posts()`
- `admin_url()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
