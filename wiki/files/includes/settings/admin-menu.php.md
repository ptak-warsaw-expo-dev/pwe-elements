# `includes/settings/admin-menu.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 3016 B
- **Liczba linii:** 122
- **Źródło:** `includes/settings/admin-menu.php`

## Klasy i metody

- Brak klas.

## Funkcje globalne

- `pwe_get_admin_pages()` — linia 9
- `pwe_elements_page()` — linia 31
- `pwe_render_admin_tabs($current_slug)` — linia 57
- `pwe_get_current_admin_page()` — linia 74
- `pwe_render_admin_page()` — linia 95

## Rejestracje WordPress / GF

- **action:** `admin_menu` — linia 7

## Wybrane wywołania statyczne

- Brak.

## API WordPress rozpoznane heurystycznie

- `add_action()`
- `esc_url()`
- `admin_url()`
- `esc_attr()`
- `esc_html()`
- `current_user_can()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
