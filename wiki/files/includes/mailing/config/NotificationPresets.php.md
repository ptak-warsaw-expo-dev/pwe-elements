# `includes/mailing/config/NotificationPresets.php`

Plik first-party PWE Elements w kategorii `mailing-dormant`.

## Metadane

- **Kategoria:** `mailing-dormant`
- **Rozmiar:** 7963 B
- **Liczba linii:** 220
- **Źródło:** `includes/mailing/config/NotificationPresets.php`

## Klasy i metody

### `PWE_NotificationPresets` — linia 4

  - `public static resend(array $overrides = [])` — linia 9
  - `public static resend_platyna(array $overrides = [])` — linia 66
  - `public static generator_wystawcow_gr1(array $overrides = [])` — linia 122
  - `public static generator_wystawcow_gr2_3(array $overrides = [])` — linia 171

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- Brak.

## API WordPress rozpoznane heurystycznie

- Brak.

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
