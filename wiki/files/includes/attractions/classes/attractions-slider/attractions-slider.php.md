# `includes/attractions/classes/attractions-slider/attractions-slider.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 18398 B
- **Liczba linii:** 419
- **Źródło:** `includes/attractions/classes/attractions-slider/attractions-slider.php`

## Klasy i metody

### `PWEAttractionsSlider` — linia 7

  - `public __construct()` — linia 9
  - `public static initElements()` — linia 16
  - `private static renderSlide($item, $slugify)` — linia 166
  - `private static ensureMinSlides(array $items, int $min)` — linia 211
  - `public static output($atts)` — linia 232

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `PWECommonFunctions::languageChecker()`
- `self::ensureMinSlides()`
- `self::renderSlide()`

## API WordPress rozpoznane heurystycznie

- `esc_url()`
- `esc_attr()`
- `esc_html()`
- `shortcode_atts()`
- `wp_enqueue_style()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
