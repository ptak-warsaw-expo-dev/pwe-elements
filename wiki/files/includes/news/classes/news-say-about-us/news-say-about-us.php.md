# `includes/news/classes/news-say-about-us/news-say-about-us.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 26578 B
- **Liczba linii:** 478
- **Źródło:** `includes/news/classes/news-say-about-us/news-say-about-us.php`

## Klasy i metody

### `PWENewsSayAboutUs` — linia 3

  - `public __construct()` — linia 4
  - `public static initElements()` — linia 5
  - `public static output($atts, $content = null)` — linia 161

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `PWECommonFunctions::decode_clean_content()`
- `self::languageChecker()`

## API WordPress rozpoznane heurystycznie

- `shortcode_atts()`
- `wp_kses_post()`
- `sanitize_text_field()`
- `esc_url_raw()`
- `wp_parse_url()`
- `home_url()`
- `esc_url()`
- `do_shortcode()`
- `get_the_ID()`
- `get_post_thumbnail_id()`
- `wp_get_attachment_image_url()`
- `esc_attr()`
- `wp_get_attachment_image()`
- `esc_html()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
