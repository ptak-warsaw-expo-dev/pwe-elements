# `includes/registration/classes/registration_potential_exhibitors.php`

Plik first-party PWE Elements w kategorii `registration`.

## Metadane

- **Kategoria:** `registration`
- **Rozmiar:** 4313 B
- **Liczba linii:** 89
- **Źródło:** `includes/registration/classes/registration_potential_exhibitors.php`

## Klasy i metody

### `PWERegistrationPotentialExhibitors` — linia 7

  - `public __construct()` — linia 13
  - `public static output($atts, $registration_type, $registration_form_id)` — linia 23

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `PWECommonFunctions::json_fairs()`
- `PWECommonFunctions::get_database_groups_data()`

## API WordPress rozpoznane heurystycznie

- `plugin_dir_path()`

## Dołączane pliki / wyrażenia include

- `_once plugin_dir_path(dirname( __FILE__ )) . 'assets/style.php'`

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
