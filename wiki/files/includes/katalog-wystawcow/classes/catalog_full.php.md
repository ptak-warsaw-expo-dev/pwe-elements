# `includes/katalog-wystawcow/classes/catalog_full.php`

Plik first-party PWE Elements w kategorii `exhibitor-catalog`.

## Metadane

- **Kategoria:** `exhibitor-catalog`
- **Rozmiar:** 8693 B
- **Liczba linii:** 181
- **Źródło:** `includes/katalog-wystawcow/classes/catalog_full.php`

## Klasy i metody

### `PWECatalogFull` — linia 2

  - `public __construct()` — linia 8
  - `public static output($atts, $identification)` — linia 18

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `PWECommonFunctions::findColor()`
- `PWECommonFunctions::adjustBrightness()`
- `CatalogFunctions::logosChecker()`
- `CatalogFunctions::checkTitle()`
- `PWECommonFunctions::languageChecker()`

## API WordPress rozpoznane heurystycznie

- `wp_get_nav_menus()`
- `get_locale()`
- `wp_get_nav_menu_items()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
