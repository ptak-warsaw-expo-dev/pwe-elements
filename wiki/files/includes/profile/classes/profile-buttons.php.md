# `includes/profile/classes/profile-buttons.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 15709 B
- **Liczba linii:** 413
- **Źródło:** `includes/profile/classes/profile-buttons.php`

## Klasy i metody

### `PWEProfileButtons` — linia 7

  - `public __construct()` — linia 13
  - `public static getImagesFromDirectory($basePath, $limit = 10, $order = false)` — linia 17
  - `public static initElements()` — linia 73
  - `public static output($atts)` — linia 152

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `parent::__construct()`
- `self::getImagesFromDirectory()`
- `self::decode_clean_content()`

## API WordPress rozpoznane heurystycznie

- `shortcode_atts()`
- `get_locale()`
- `wp_get_attachment_url()`
- `site_url()`
- `esc_url()`

## Dołączane pliki / wyrażenia include

- `s/profile/media/visitor_profile_icon_white.webp'`
- `s/profile/media/exhibitor_profile_icon_white.webp'`
- `s/profile/media/industry_scope_icon_white.webp'`

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
