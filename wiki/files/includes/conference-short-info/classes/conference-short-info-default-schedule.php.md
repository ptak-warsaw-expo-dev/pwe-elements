# `includes/conference-short-info/classes/conference-short-info-default-schedule.php`

Plik first-party PWE Elements w kategorii `module`.

## Metadane

- **Kategoria:** `module`
- **Rozmiar:** 20178 B
- **Liczba linii:** 465
- **Źródło:** `includes/conference-short-info/classes/conference-short-info-default-schedule.php`

## Klasy i metody

### `PWEConferenceShortInfoDefaultSchedule` — linia 3

  - `public static initElements()` — linia 5
  - `private static is_mobile_device()` — linia 9
  - `public static output($atts, $all_conferences, $rnd_class, $name, $title, $desc)` — linia 22

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych literalnych rejestracji.

## Wybrane wywołania statyczne

- `self::getFairDaysFromShortcodes()`
- `self::getConferenceOrganizer()`
- `self::parse_conference_key_to_date()`
- `PWECommonFunctions::languageChecker()`
- `self::is_mobile_device()`
- `PWEConferenceShortInfo::multi_translation()`
- `PWESwiperScripts::swiperScripts()`

## API WordPress rozpoznane heurystycznie

- `current_user_can()`
- `do_shortcode()`
- `esc_attr()`
- `esc_url()`
- `esc_html()`
- `plugin_dir_path()`

## Dołączane pliki / wyrażenia include

- `_once plugin_dir_path(__FILE__) . '/../../../scripts/swiper.php'`

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
