# `gf-addons/area-numbers/area-frontend.php`

Plik first-party PWE Elements w kategorii `gravity-forms-addon`.

## Metadane

- **Kategoria:** `gravity-forms-addon`
- **Rozmiar:** 3219 B
- **Liczba linii:** 85
- **Źródło:** `gf-addons/area-numbers/area-frontend.php`

## Klasy i metody

### `GFAreaNumbersFrontend` — linia 3

  - `__construct()` — linia 5
  - `area_enqueue_scripts($form, $is_ajax)` — linia 9

## Funkcje globalne

- `field_disabled($content)` — linia 80

## Rejestracje WordPress / GF

- **action:** `gform_enqueue_scripts` — linia 6

## Wybrane wywołania statyczne

- Brak.

## API WordPress rozpoznane heurystycznie

- `add_action()`
- `add_filter()`
- `plugins_url()`
- `plugin_dir_path()`
- `wp_enqueue_style()`
- `wp_enqueue_script()`
- `wp_localize_script()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / właściwości `$wpdb`

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole i hooki są wyciągane tokenizatorem PHP.
- Sekcje zależności i include są statyczną heurystyką i mogą nie widzieć dynamicznych ścieżek.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
